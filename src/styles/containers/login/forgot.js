import defaultStyles from './styles';

const styles = {
  ...defaultStyles,
  inputForm: {
    marginTop: '20px',
    marginBottom: '0px',
  },
  messageLogin: {
    textAlign: 'center',
    marginTop: '15px',
  },
  acceptBtn: {
    marginTop: '30px',
  },
};

export default styles;
